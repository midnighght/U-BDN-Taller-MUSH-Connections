# Vite Environment Variables
# Note: In Vite, only variables prefixed with VITE_ are exposed to the client

# Backend API URL
VITE_API_BASE_URL=http://localhost:3000

# Frontend Development Server Port (configured in vite.config.ts)
VITE_PORT=5174
